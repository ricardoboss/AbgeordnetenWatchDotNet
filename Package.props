<Project>

	<!-- NuGet package properties -->
	<PropertyGroup>
		<PackageVersion>2.0.0</PackageVersion>
		<Description>An SDK for the abgeordnetenwatch.de API.</Description>
		<PackageTags>AbgeordnetenWatch;API;SDK;kiota</PackageTags>

		<Authors>Ricardo Boss</Authors>
		<Copyright>Copyright © 2025-2026 Ricardo Boss</Copyright>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<PackageProjectUrl>https://github.com/ricardoboss/AbgeordnetenWatchDotNet</PackageProjectUrl>

		<RepositoryUrl>https://github.com/ricardoboss/AbgeordnetenWatchDotNet</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PublishRepositoryUrl>true</PublishRepositoryUrl>

		<IncludeSymbols>true</IncludeSymbols>
		<SymbolPackageFormat>snupkg</SymbolPackageFormat>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)' == 'Release'">
		<GeneratePackageOnBuild>true</GeneratePackageOnBuild>
		<PackageOutputPath>$(MSBuildThisFileDir)../publish</PackageOutputPath>
	</PropertyGroup>

	<Target Name="PreparePackageReleaseNotesFromFile" BeforeTargets="GenerateNuspec">
		<ReadLinesFromFile File="../CHANGELOG.md">
			<Output TaskParameter="Lines" ItemName="ReleaseNoteLines" />
		</ReadLinesFromFile>
		<PropertyGroup>
			<PackageReleaseNotes>@(ReleaseNoteLines, '%0a')</PackageReleaseNotes>
		</PropertyGroup>
	</Target>

	<ItemGroup Condition="'$(IsPackable)' == 'true' And '$(Configuration)' == 'Release'">
		<None Include="../LICENSE.md" Pack="true" PackagePath="\" />
		<None Include="$(ReadmePath)" Pack="true" PackagePath="\" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.103">
			<PrivateAssets>all</PrivateAssets>
			<IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
		</PackageReference>
	</ItemGroup>
</Project>
